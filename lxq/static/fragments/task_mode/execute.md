# Execute mode

- Confirm input state and predecessor artifacts before running downstream tools.
- Verify installed versions and actual function/CLI signatures.
- Record environment, references, parameters, seeds, execution order, logs, and outputs.
- Use small validation runs when feasible before full computation.
- Never substitute mock data or silently switch methods when required inputs or runtimes are missing.

